name: Release_Tags
on: [workflow_call]
jobs:
  CI_Steps:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4 #v4 is version of checkout action release by git actions

      - name: SAST Scans
        run: echo SAST Scans

      - name: SCA Scans
        run: echo SCA Scans

      - name: Detect Secrets in Source Code
        run: echo Detect Secrets in Source Code

      - name: Produce Artifact
        run: echo Produce Artifact


